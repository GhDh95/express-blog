<div class="md:flex md:flex-row flex flex-col flex-col-reverse justify-around md:p-20">
    <div class="text-2xl flex flex-col space-y-2 items-center max-h-screen overflow-y-auto">

        <p class="sticky top-0 bg-purple-200 p-2 rounded-sm w-full">
            posts
        </p>

        <% posts.forEach((post) => { %>
            <%- include('components/post', { post: post }) %>
        <% }) %>




    </div>
    <div>
        <%- include ('components/form.ejs') %>
    </div>
</div>